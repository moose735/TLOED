// tailwind.config.js (Example - you would create this file if it doesn't exist)
module.exports = {
  theme: {
    extend: {
      colors: {
        'primary-blue': '#2563EB', // Your primary brand color
        'secondary-green': '#10B981', // Another brand color
        'accent-yellow': '#F59E0B',
        // Define your own shades based on your theme
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'], // Example custom font
      },
      // You can extend other properties like spacing, fontSize, etc.
    },
  },
  plugins: [],
};
