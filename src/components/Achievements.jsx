import React from 'react';

// Achievements UI removed per user request. This stub intentionally renders nothing
// to avoid import errors from other modules that might still import this component.
const Achievements = () => null;

export default Achievements;
